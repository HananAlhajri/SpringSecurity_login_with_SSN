spring:
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
            format_sql: true
        show-sql: true
    datasource:
        url: jdbc:mysql://localhost:3306/test
        username: root
        password: password

server:
    port: 8080
